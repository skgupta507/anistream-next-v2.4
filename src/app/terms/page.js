export const metadata = {
  title: "Terms of Service — AnimeDex",
  description: "AnimeDex terms of service — the pact you agree to when entering the abyss.",
};
import TermsClient from "./TermsClient";
export default function TermsPage() { return <TermsClient />; }
